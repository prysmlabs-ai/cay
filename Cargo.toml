# An open, from-scratch userspace runtime for the Coral Edge TPU.
# cay: DarwiNN executable reader (cay::program) + USB transport and execution.
[workspace]
resolver = "2"
members = ["cay"]
exclude = ["cay-py"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
